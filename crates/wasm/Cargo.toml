[package]
name = "platlab_wasm"
version = "0.1.0"
edition = "2021"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[lib]
crate-type = ["cdylib"]

[dependencies]
platlab_core = { path = "../core" }
wasm-bindgen = "0.2"
js-sys = "0.3"
